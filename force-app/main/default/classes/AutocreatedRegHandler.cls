//TODO:This autogenerated class includes the basics for a Registration
//Handler class. You will need to customize it to ensure it meets your needs and
//the data provided by the third party.

global class AutocreatedRegHandler implements Auth.RegistrationHandler{

//Method used to return user details when federationidentifier equal GDI identifier for connected user
global User createUser(Id portalId, Auth.UserData data){
    
    List<User> users = [Select Id From User Where federationidentifier =: data.identifier];
    
    if (users.size() == 1) 
    {
      return users[0];
    } 
    else 
    {
      return null;
    }
    
}

//Method used to update user data but not used in our case
global void updateUser(Id userId, Id portalId, Auth.UserData data){
  //Todo something in this case
}
}